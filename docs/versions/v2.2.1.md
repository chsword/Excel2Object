# v2.2.1

发布日期：2026-09-11

## 概述

本版本为命令行工具的发布修复，库代码未作改动。

## 变更明细

v2.2.0 的命令行工具包体超过 NuGet 250 MB 的上限，未能发布成功。原因在于 NPOI 会为每个运行时标识符引入 SkiaSharp 的本机依赖，多目标框架打包后体积成倍增长。本版本将命令行工具的目标框架收敛为 net8.0，并剔除本机 `.pdb` 文件，使包体回到限制以内。

安装方式不变：

```bash
dotnet tool install -g Chsword.Excel2Object.Cli
```
